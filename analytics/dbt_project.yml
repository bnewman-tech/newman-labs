name: newman_labs
version: "1.0.0"
config-version: 2

profile: newman_labs

model-paths: ["models"]
test-paths: ["tests"]

models:
  newman_labs:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: view
      houston_signal:
        +schema: houston_signal

clean-targets:
  - target
  - dbt_packages
